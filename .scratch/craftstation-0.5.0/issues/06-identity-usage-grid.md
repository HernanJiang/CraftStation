# 06 — Identity、alias 与 Account Row 2×2

**What to build:** 每个账号显示真实 identity 主文字、可编辑 alias 副文字和固定 2×2 quota/token/cache 视图模型。

**Blocked by:** 02 — Account Pool scheduling contract；05 — Provider presentation order 与 4/1 UI

**Status:** ready-for-agent

- [ ] 默认 alias 为 `<Provider> Account N`，编辑后重启保持
- [ ] Account Row 顺序/优先级文案与 Provider 展示顺序语义分离
- [ ] 无可靠 cache 数据显示 `—`，不伪造比例
